import { IUseCase } from '@bookstore-nx/ddd-core';
import { Injectable } from "@nestjs/common";
import { CommandBus } from '@nestjs/cqrs';
import { <%= dto.className %>Dto } from '../shared';

@Injectable()
export class CreateBookUseCase implements IUseCase<<%= dto.className %>Dto, void>
{
  constructor(readonly commandBus: CommandBus) { }

  async executeAsync(input: <%= dto.className %>Dto): Promise<void>
  {
    const command: CreateBookCommand = CreateBookCommand.create(input);
    return this.commandBus.execute(command);
  }
}
