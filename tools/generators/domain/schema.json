{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "domain",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "domain name",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
