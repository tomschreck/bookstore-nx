{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "SignalId",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "enter name of signal id type: `firm signal id` or 'annuity update signal id'",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    }
  },
  "required": ["name"]
}
